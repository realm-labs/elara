# Differential Tests

This directory is reserved for tests that compare Elara behavior with the
reference implementation for the current stable Lua release.

Differential tests should document the reference command, fixture inputs, and
any intentionally unsupported behavior.
