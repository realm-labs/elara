# Conformance Tests

This directory is reserved for tests that check Elara against the current stable
Lua language specification.

Conformance cases should avoid old-version compatibility behavior unless the
architecture is intentionally revised.
