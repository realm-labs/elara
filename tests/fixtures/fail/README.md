# Failing Lua Fixtures

This directory stores Lua source fixtures that should produce diagnostics for
the current stable Lua target.

Each future fixture should make the expected failure mode clear through its file
name and, when snapshot support exists, through an accompanying diagnostic
snapshot.
