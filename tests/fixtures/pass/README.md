# Passing Lua Fixtures

This directory stores Lua source fixtures that should parse, compile, and run
successfully for the current stable Lua target.

Keep fixtures small and focused. When expected output or bytecode snapshots are
needed, add them in the snapshot baseline step instead of encoding them here.
